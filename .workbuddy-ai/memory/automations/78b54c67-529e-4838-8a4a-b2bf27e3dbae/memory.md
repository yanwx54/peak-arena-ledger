# 自动化执行记录：PeakArena Ledger 每日更新

## 2026-09-23 10:02
- **结果**：成功（脚本 2m36s；另跑一轮 `--skip-cookie --skip-feishu` 验证新步骤，2m06s）
- **无新期**：wxredian 归档仍止于 2026-09-20，文章 277 篇与上轮一致 → 首跑脚本报「无变更，跳过提交」
- **但发现并修复了静默漏抓**：`extract_jd.py` 表头正则写死 `【华府卫视…战报】`，
  而源头表头另有 `【华府昨日战报】`（省略「卫视」）和整段漏写 `【】` 两种写法，
  导致 2026-03-28、2026-09-20 两期战报被整段跳过，漏 3 场。
  修复：表头放宽 + 无表头时以编号 `N、巅峰赛场` 兜底。全量 277 篇 diff 校验：+4 条、0 丢失、0 误增。
- **数据**：204 → **207 场**（2026-01-05 ~ 2026-09-19）
- **飞书**：覆盖上传成功（version 7688547559391988930）
- **GitHub**：`04d2337` → `c6b59b4` → `1ab88e7`，已推送，本地与远程一致
- **顺带修**：① `daily_update.py` 新增「同步根目录交付物」步骤（根 CSV 自建库起就是误命名的 xlsx 二进制，HTML 停在 204 场）；
  ② `diag.py` 补「整段被跳过」检查（原检查有盲区，本次缺陷它报 0 遗漏）

### 下次注意
- 枚举文章数仍是 277，说明**归档站还没收录新一期**。若连续多日不变属正常，不必反复重跑。
- 若出现新的表头写法，整段会被静默跳过 → 先跑 `diag.py`（A/B 两项都应为 0）定位。
