# 2026-09-23 工作日志

## 任务：定时更新（自动化 78b54c67）—— 结果：跑通，并修掉一个静默漏抓缺陷

### 流水线执行
`daily_update.py` 全链路跑通（2m36s）：cookie 刷新 → 枚举 277 篇 → 抓正文 → 解析 → 出表 → xlsx → 飞书覆盖上传 → GitHub 推送。

**本轮无新期**：wxredian 归档仍止于 2026-09-20，文章数 277 与上轮一致。新一期还没进归档站。

### 发现的缺陷：解析器整段跳过两期战报（重要）
首跑报「无变更」，但抽查最新文章时发现 **2026-09-20 那篇明明有 `1、巅峰赛场 BO7 / Speed 4:1 JD`，却没产生任何记录**。

追根因：`extract_jd.py` 的战报段表头正则写死 `【华府卫视…战报】`，而源头表头有三种写法：
1. `【华府卫视昨日战报】` —— 主流（194 次）
2. `【华府昨日战报】` —— 省略「卫视」（2025-12-18、2026-09-20）
3. **整段漏写 `【】` 表头**（2026-03-28，正文直接从叙述接 `1、巅峰赛场 bo7`）

写法 2、3 导致整个战报段不被匹配 → 段内战绩全丢。共漏 3 场（口径内）：
- `2026-03-27 JD 4:0 Bisu`（主赛制 BO7）
- `2026-03-27 JD 1:2 Bisu`（加赛）
- `2026-09-19 Speed 4:1 JD`

### 修复与验证
- 表头放宽为 `【\s*华府(?:卫视)?[^】]{0,10}战报\s*】`
- 新增 `parse_unanchored()`：正文无匹配战报段时，以编号 `N、巅峰赛场` 行为锚点解析（仅当正文提到「华府」时启用，防误收）
- **验证方式**：全量 277 篇逐条 diff 修正前后的原始记录 → 恰好 +4 条、**0 条丢失、0 条误增**（+4 中 1 条属 2025-12-17，被 2026-01-01 起算日正确滤掉）
- **加赛行是否该收**：查既有数据，已有 21 例「同日同对选手两条」（如 2026-01-26 `Soma 4:1 Mini` + `Soma 2:1 Mini`），故 `JD 1:2 Bisu` 符合既有惯例，收入

### 顺带修的两处
- **根目录交付物长期腐化**：README 声明根目录 CSV/HTML 是交付物，但没有任何步骤刷新它们。
  根 `华府卫视巅峰赛场战绩_2026.csv` 自建库首个提交起就是**被误命名的 xlsx 二进制**（PK 头），HTML 停在「共 204 场」。
  → `daily_update.py` 新增「同步根目录交付物（CSV / HTML）」步骤，从 `output/` 复制。已重新生成，根 CSV 现为真 CSV（207 行）。
- **自检形同虚设**：`diag.py` 只检查「已匹配到的战报段内部」，整段被跳过时报 0 遗漏 —— 正是本次缺陷的盲区。
  → 补第二道检查「全文含『巅峰赛场』却零解析」，现在 A/B 两项均为 0 才算通过（已跑，退出码 0）。

### 结果
- **207 场**（2026-01-05 ~ 2026-09-19），原始记录 459 条
- 按月：1月17 / 2月16 / 3月23 / 4月35 / 5月24 / 6月25 / 7月31 / 8月20 / 9月16
- 飞书台账已覆盖上传（链接恒定）
- GitHub 推送：`04d2337`（数据+解析器）→ `c6b59b4`（根目录交付物同步）→ `1ab88e7`（README + diag），本地与远程一致，工作区干净
- README 已同步更新数据范围、原始记录数、解析器要点、已知边界
